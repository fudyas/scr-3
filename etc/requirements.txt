# md pdf tool
Markdown>=3.5
Pygments>=2.17
weasyprint>=62.0

# dev tools (query-llm)
pydantic>=2.7
pydantic-ai[anthropic]>=0.0.13
# Retry transient LLM failures (timeouts, rate limits, transient 5xx) in query-llm
tenacity>=8.0

# backend
fastapi>=0.111.0
uvicorn>=0.29.0
# Multipart form parsing for the SET-AVATAR image upload (BLG-0815); mirrors the accounts service reqs.
python-multipart>=0.0.9
python-socketio[asyncio_client]>=5.11.0
aiofiles>=25.0.0
dotenv
aioshutil
httpx>=0.27.0
colorama

# CLI
rich>=13.7.0
click>=8.1.0
questionary>=2.0.1

# PostgreSQL (authoring conversation store + document STE): SQLAlchemy async core,
# asyncpg driver, Alembic migrations. The dev/host venv mirrors the services so the
# authoring composition root + the host-side inspect/conversation tools import and run.
SQLAlchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13

# Identity: the dev/host venv mirrors the Accounts service so its Cognito-stub JWT sign/verify code
# (src/core/lib/identity) imports + its unit tests run on the host (mirrors src/app/services/accounts/etc).
PyJWT>=2.8

# Optional heavy deps baked into the service images, mirrored here so a bare host
# `lets scr test suite --unit` collects clean (the canonical unit gate). fastembed
# backs the retrieval/indexing cross-encoder + bm25 unit tests (mirrors
# src/app/services/{indexing,retrieval}/etc/requirements.txt); fpdf2 backs the
# handoff Markdown→PDF export unit tests (mirrors src/app/services/handoff/etc).
# Neither downloads a model at import — the reranker unit tests inject a fake encoder.
fastembed>=0.4.0
fpdf2>=2.8

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
