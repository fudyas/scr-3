interface:
  display_name: "Git Push"
  short_description: "Preview and safely push a branch over SSH"
  default_prompt: "Use $git-push to preview and push the current branch."
policy:
  allow_implicit_invocation: false
