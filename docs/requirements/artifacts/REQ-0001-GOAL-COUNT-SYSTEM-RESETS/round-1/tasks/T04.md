# T04: exact hardware automatic boot and cleanup

1. REQUIRE T03 independent PASS before hardware use.
2. VERIFY exact target identity, recovery readiness, protected hashes, package/runtime/native graph/link/record/taint baseline.
3. INSTALL exact canonical DEB without target APT/network dependency change or module autoload.
4. VERIFY native activation gates; TEST manual start, repeated start, ordinary unload/reload, faults/CLI.
5. REBOOT once ordinarily only after every preflight gate PASS.
6. REQUIRE module, guard, device, expected logs, exactly `+1` record, no duplicate/anomaly/new taint.
7. REMOVE/PURGE through ordinary unload; REMOVE transient artifact; VERIFY exact baseline and connectivity.
8. ***if*** any failure occurs ***then*** STOP, preserve evidence/quarantine; never force, cleanup reboot, direct graph edit, kernel/U-Boot change.
