# T04 — Durable records

Depends on T01,T02,T03. Implement secure module VFS layer, names, create/sync/reason ordering, safe iteration/unlink primitives.

Own: record store/name sources/headers, record fault tests, `evidence/durable-records/*`.

DoD: safe approved root; no symlink escape; exact name grammar; four lowercase random letters; exclusive create; <=64 collisions; directory/file modes exact; new directory parent synced; empty inode + directory synced before reason; same inode retained; reason failure still counts; precise RO/ENOSPC/EIO/sync states; immediate matching regular files only; unsafe symlink/hardlink preserved/error; unrelated files untouched. Software ordering proven, physical durability deferred.

Tests: faults at each boundary; collisions; bad clocks/ranges; storage failures; inode replacement; concurrent path attacks; malformed names; unsafe links; independent ordering trace.
