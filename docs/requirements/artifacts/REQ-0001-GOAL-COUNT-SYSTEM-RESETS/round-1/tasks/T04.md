# T04: bounded real-hardware proof

On `192.168.68.55`, verify identity/baseline. Install exact DEB; test manual start, repeated start, unload/reload, fault hooks. Restore installed baseline. Perform one approved ordinary reboot. Require automatic module/guard/device and count delta exactly `+1`, no duplicate/anomaly/taint change. Remove/purge, ordinary unload, delete transient, verify exact baseline restoration and connectivity.
