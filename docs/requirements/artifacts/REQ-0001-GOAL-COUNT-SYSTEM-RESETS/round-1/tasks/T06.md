# T06 — Bash frontend and boot activation

Depends on T02,T03,T05. Implement `/usr/sbin/scr-resets-monitor`, one-shot SysV loader, exact rcS symlink, README.

Own: four payload paths, frontend/boot tests, `evidence/bash-frontend-boot/*`.

DoD: Bash validates shape/transports/displays only; quoted builtins; no `eval`, date math, enumeration, deletion, locks, retry, cache, marker, module load, or fallback; missing/incompatible endpoint fails; uncertain reset response not replayed. Loader checks pending-removal + exact kernel, then loads private module; loader never reads/writes guard or records; failed load permits boot; config/upgrade never load/reload; plural public names only; no reboot/service/MONIT change.

Tests: stub protocol responses; quoting/metacharacters/newlines; all CLI shapes; authority static audit; loader wrong-kernel/missing-module/failure/pending/repeat; offline/reconfigure/upgrade no-load.
