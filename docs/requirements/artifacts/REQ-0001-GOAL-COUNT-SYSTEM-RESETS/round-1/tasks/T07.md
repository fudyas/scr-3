# T07 — Integrated tests and DEB

Depends on T02,T03,T04,T05,T06. Integrate exact inputs, build one target-architecture DEB, package-test, provenance, full nonhardware suite.

Own: build/dist, module payload, package-test, integration tests, `evidence/tests-deb/*`.

DoD: reproducible ABI/source/toolchain hashes; wrong ABI/dependency rejected; one package only; exact payload; all component/fault/lifecycle tests pass; packaged test never loads ARM module into unrelated host; metadata/restoration supplements cover timestamps/hardlinks/ACL/xattrs/capabilities/parents; no skipped claim marked passed; physical HW gaps explicit; exact DEB SHA ready.

Tests: A01–A11,A15; record fault/race matrix; protocol/frontend matrix; install/remove/purge/reinstall/upgrade/failure/interruption; overlap; package content/hash/dependencies; host mismatch.
