# T08 — Locked image test

Depends on T07. Test exact DEB on tooling-managed disposable copy of `var/image_8.26.0` under one continuous SDLC lock.

Own: `evidence/locked-image-test/*` only.

DoD: source hash recorded; no quarantine/conflict; copy mounted inside same lock; baseline + supplemental metadata captured; exact artifact installed/tested/uninstalled even after functional failure; all managed paths/original records restored; generated/runtime/backup/ownership residue absent; unrelated data preserved; unmount succeeds; source hash unchanged. Any uninstall/restoration/unmount/source failure quarantines identity and stops reuse; no manual recovery. Environment boundary and HW-deferred claims explicit.

Tests: normal full cycle; functional failure cleanup; injected uninstall/restoration/unmount/source failures; quarantine reuse refusal; metadata comparison; A09,A11–A15.
