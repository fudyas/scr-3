# T02: bounded SDLC recovery safety

1. ADD explicit recovery path binding original source hash, existing copy/root identity, preserved first baseline, installed artifact, backup digest, quarantine identity, phase journal.
2. REQUIRE real acquired image lock; environment flags alone never authorize recovery.
3. REFUSE fresh fingerprint from dirty root, source-copy overwrite, missing/corrupt baseline, wrong image/hash, unrelated-marker clearance.
4. CLEAR matched quarantine only after corrected upgrade/remove/purge, exact restoration, unmount/loop release, source-hash PASS.
5. RETAIN quarantine and evidence at every failure/crash gate.
6. REMOVE unowned offset fixture mutation; add tooling tests/help/guide only where interface changes.
7. VALIDATE without image/HW mutation; record exact evidence.
