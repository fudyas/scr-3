# T02: verified clone recovery tooling

1. IMPLEMENT authentic-source validation without source mount.
2. CREATE independent tooling-owned clone under real lock.
3. VERIFY clone distinct path/inode/single link, exact size, pre-mount SHA-256 equals approved source.
4. MOUNT clone rw at offset `50331648`, size `3633315840`; allow ordinary ext4 journal replay only.
5. UNMOUNT clone cleanly; VERIFY loop release; RECORD post-replay hash.
6. MOUNT replayed clone ro; CAPTURE semantic fingerprint/protected hashes; UNMOUNT; VERIFY loop release and unchanged ro-inspection hash.
7. VERIFY authentic source identity/hash before/after; never mount source.
8. RETAIN one lock through failed-copy upgrade/test/remove/purge/restoration and outer-only marker clearance.
9. ADD clone alias/hash/order/failure tests and exact recovery guide/agent instructions.
10. ***if*** any gate fails ***then*** RETAIN quarantine/copy/clone/baseline/fixtures/journal and STOP.
11. VALIDATE without real image/HW mutation.
