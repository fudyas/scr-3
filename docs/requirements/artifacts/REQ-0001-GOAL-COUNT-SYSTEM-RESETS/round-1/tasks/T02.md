# T02: package-owned legacy migration

1. IMPLEMENT `1.0.4` recovery-only `preinst` migration bound to T01 source/fingerprint/report/digest.
2. REQUIRE package-owned atomic/durable controls; never alter/recapture legacy members.
3. IMPLEMENT outer unchanged-legacy/new-control proof and interruption recovery.
4. PRESERVE runtime/module/native-oracle behavior.
5. UPDATE docs/agent contracts/RELEASE/package-state.
6. RUN static, package, digest, migration, fault, lifecycle, and tooling tests.
7. BUILD twice; require byte-identical `armhf` DEB; record exact DEB/module hashes.
8. STOP before real recovery mutation.
