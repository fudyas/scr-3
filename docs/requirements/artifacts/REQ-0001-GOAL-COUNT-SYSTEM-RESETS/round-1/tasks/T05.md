# T05 — Module control protocol

Depends on T03,T04. Implement root-only misc device, bounded text protocol, module UTC parser, count/reset operations.

Own: control/admin/time sources/headers, `contracts/control-v1.md`, protocol tests, `evidence/module-control/*`.

DoD: `0600` endpoint + kernel privilege recheck; <=256 bytes including newline; partial-write accumulation; one validated command/session; bounded response; malformed/NUL/extra/incomplete input cannot mutate; strict UTC parser with omitted fields zero/inclusive bound; count includes empty/partial files; reset cancels pending before delete and sync; partial failures truthful; count preserves pending; client death releases resources; lost response never replays; guard/hardware/backup untouched.

Tests: every fragmentation boundary; request limits; privilege; dates/overflow; absent/unsafe directory; concurrent worker/count/reset; partial delete; client death/lost response; unload pinning.
