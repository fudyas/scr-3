# T05: unchanged hardware acceptance

1. REQUIRE T04 independent PASS and exact artifact/runtime/target preflight.
2. INSTALL exact DEB on `192.168.68.55` without target APT/network; verify native graph/link oracle.
3. VERIFY manual start exactly `+1`; repeated start/unload/reload `+0`; CLI/sync-fault checks PASS.
4. RUN one ordinary reboot; require module/guard/device and exactly `+1`, no duplicate/anomaly/new taint.
5. REMOVE and purge ordinarily; require exact target records/graphs/links/metadata/protected/package/connectivity restoration.
6. NEVER force, cleanup reboot, persist credentials, or direct-edit graph. RECORD exact HW evidence/follow-up gate.
