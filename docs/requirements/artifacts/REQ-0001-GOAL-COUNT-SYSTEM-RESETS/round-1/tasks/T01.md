# T01: read-only legacy backup inspection

1. IMPLEMENT explicit recovery inspection mode and failure-retention tests.
2. BIND exact source/copy/root/markers/replay baseline/fixtures/geometry/state root.
3. HOLD real quarantine-aware lock across read-only failed-copy mount, inspection, clean unmount, loop/source proof.
4. AUTHENTICATE exact `1.0.2` backup layout, members, claims, graphs, parents, records, owners, tools, and protected files against replay/source baseline.
5. COMPUTE deterministic canonical digest; persist external report/journal.
6. REQUIRE quarantine unchanged; no dpkg/image write/marker clearance.
7. ***if*** any check/cleanup fails ***then*** RETAIN quarantine/evidence and STOP.
