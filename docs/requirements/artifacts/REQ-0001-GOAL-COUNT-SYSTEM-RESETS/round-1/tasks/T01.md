# T01: private immutable broker

1. RETAIN independently validated revision-13 pinned runtime; reject pin/generation drift.
2. REPLACE sealed memfd transport with real-root bounded broker child using propagation-private mount+network namespace.
3. SELF-BIND exact named QEMU/DEB canonical files; remount exact files read-only with safety flags; verify exact mountinfo/identity and no parent propagation.
4. PROVE content/metadata immutability against full approved mutation/FD/alias matrix; any weak/ambiguous result stops and replans.
5. RUN exact ARM probe SHA-256 `faef115dbbfbf07356b21d016b80c9e9889634f71cab1ca8d67cf651d8d350e8`; require exit `42`.
6. TEST setup faults, signals/timeouts/descendants, namespace survival, cleanup, parent snapshots; require zero residue and independent validation. No image mutation.
