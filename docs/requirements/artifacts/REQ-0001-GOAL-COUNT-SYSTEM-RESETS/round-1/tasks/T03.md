# T03: quarantined recovery and fresh disposable lifecycle

1. REQUIRE T01/T02 independent PASS.
2. VERIFY exact source, failed copy/root, marker pair, fixtures, package, offset, size, state-root bindings.
3. RUN one approved recovery through `./bin/lets sdlc recover-package` under continuous real lock.
4. REQUIRE verified clone rw replay + ro baseline before failed-copy mutation.
5. UPGRADE broken installed hooks through ordinary dpkg to pinned `1.0.3`; TEST package; REMOVE; PURGE.
6. REQUIRE exact replayed-clone restoration, preserved immutable backup, clean unmount/loop release, source hash exact.
7. ALLOW tooling-only matched-quarantine clearance after every gate PASS.
8. RUN fresh disposable install/test/remove/purge/restoration lifecycle under continuous lock only after recovery PASS.
9. ***if*** any cleanup/restoration/unmount/loop/source gate fails ***then*** RETAIN quarantine and STOP.
10. RECORD exact journal, clone hashes/fingerprint, native graph/link, artifact, lock, restoration evidence.
