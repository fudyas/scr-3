# T01 implementation brief

Requirement: `REQ-0001-GOAL-COUNT-SYSTEM-RESETS`

Round: `1`

Approved plan SHA-256: `9ecccb712b5813c5c735da44697f404f576c51fd18639ba66afd07eb0fc719f4`

Definitions SHA-256: `6849f6f0c9ce74f22c69b14435347a1dddfa3ab238179c06b6f8695ccbef529d`

$caveman full. Implement `T01` only. Preserve exact technical substance.

## Validated predecessors

None.

## Contract

# Revision 10 contract

Implement approved revision 10 SHA-256 `9ecccb712b5813c5c735da44697f404f576c51fd18639ba66afd07eb0fc719f4`. Preserve revision-8 validated module behavior, approximate ABI waiver, one reversible `armhf` DEB, ordinary module operations, continuous image lock, restoration/quarantine, exclusions. Treat Jessie `insserv -s` diagnostic only. Registration authority: native rc, exact links, generated graph membership/order. Unregistration authority: native rc, zero links, exact immutable `.depend.start`, `.depend.stop`, `.depend.boot` baseline. Never directly edit graph content, target APT, kernel/U-Boot, MMIO/reason, force module operations, credentials, image, ledger, quarantine marker. Recover existing quarantined copy only through bounded SDLC tooling using authentic original baseline; preserve quarantine on any failure.

## Interfaces

# Revision 10 interfaces

- Package: `scr-req-0001-goal-count-system-resets` version `1.0.3`, `Architecture: armhf`; one canonical reproducible DEB.
- Init: `/etc/init.d/scr-resets-monitor`; exact approved LSB header; `$local_fs` only.
- Register: `/usr/sbin/update-rc.d scr-resets-monitor defaults`; verify rc, exact all-runlevel links, `.depend.start` exact token/order, expected `.depend.stop`, no `.depend.boot` activation.
- Unregister: `/usr/sbin/update-rc.d -f scr-resets-monitor remove`; verify rc, zero links, exact three-graph original content/type/metadata. Never write graph content.
- Diagnostic: `/sbin/insserv -s`; capture only; never pass/fail membership.
- Recovery: explicit `./bin/lets sdlc ... --recover` path binds existing copy/root, source, original baseline, artifact, backup, quarantine, lock. Outer transaction alone clears matched quarantine after remove/purge, restoration, unmount/loop release, source-hash PASS.
- Fresh image: `./bin/lets sdlc test-package ...`; lock spans copy/mount/install/test/remove/purge/restoration/unmount/source proof.
- Hardware: exact target `192.168.68.55`; one bounded reboot only after T03 PASS; ordinary cleanup restores baseline.

## Task

# T01: corrected native lifecycle and reproducible package

1. RETAIN revision-8 validated module, CLI, retry, ABI waiver, safety, exclusions.
2. REMOVE every `insserv -s` verdict; retain diagnostic capture only.
3. IMPLEMENT authoritative register checks: native rc, exact runlevel links/targets, exact `.depend.start` token/order, expected `.depend.stop`, no unintended `.depend.boot` activation.
4. IMPLEMENT authoritative unregister checks: native rc, zero all-runlevel links, exact original three-graph content/type/metadata; never directly write graph content.
5. IMPLEMENT upgrade/abort/broken-`1.0.2` recovery, loaded/absent module cleanup, immutable original baseline.
6. BUILD canonical `1.0.3_armhf.deb` twice; require identical module/DEB hashes and static gates.
7. ADD native/Jessie fixtures and lifecycle tests.
8. STOP before image/HW mutation; record exact evidence.
