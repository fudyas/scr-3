# T01 implementation brief

Requirement: `REQ-0001-GOAL-COUNT-SYSTEM-RESETS`

Round: `1`

Approved plan SHA-256: `10ea6fc3aaca30de4d9ee0ec59f35a089869cba0403537b77be8c3d3f4a97bf9`

Definitions SHA-256: `cd221fbd99eb40ffc50fa11f56e936a12449031377e4e95a6341f4186712715c`

$caveman full. Implement `T01` only. Preserve exact technical substance.

## Validated predecessors

None.

## Contract

# Revision 9 contract

Implement approved revision 9 SHA-256 `10ea6fc3aaca30de4d9ee0ec59f35a089869cba0403537b77be8c3d3f4a97bf9`. Retain revision-8 validated module, retry, package, ABI-waiver, restoration, and exclusion behavior. Change activation only: target-native `update-rc.d`/`insserv`, `$local_fs` dependency, generated-graph proof, native unregister, exact restoration. Never write `.depend.*` content directly. Never force module operation, change global concurrency, use target APT, patch kernel/U-Boot, persist credentials, or release image lock before verified restoration.

## Interfaces

# Revision 9 interfaces

- Init: `/etc/init.d/scr-resets-monitor`; `Required-Start: $local_fs`; `Default-Start: 2`; `Default-Stop: 0 1 6`.
- Register: `/usr/sbin/update-rc.d scr-resets-monitor defaults`.
- Unregister: `/usr/sbin/update-rc.d -f scr-resets-monitor remove`.
- Verify: `/sbin/insserv -s`, runlevel links, `/etc/init.d/.depend.start`, `.depend.stop`, `.depend.boot`.
- Package: one reproducible `Architecture: armhf` DEB.

## Task

# T01: native SysV activation and proof

1. RECORD native graph, link, metadata, package, module, image, and target baselines.
2. REPLACE direct runlevel-link publication with `update-rc.d`/`insserv` registration.
3. REQUIRE exact service, link, and startpar graph membership.
4. UNREGISTER through native tooling while init script exists.
5. REQUIRE exact graph, link, metadata, package, image, and target restoration.
6. BUILD canonical `armhf` DEB twice.
7. TEST full lifecycle under continuous image lock.
8. ***if*** cleanup or restoration fails ***then***
   1. QUARANTINE image or target.
   2. STOP pipeline.
9. TEST one bounded hardware reboot.
10. REQUIRE count delta exactly `+1`.
