# T03 implementation brief

Requirement: `REQ-0001-GOAL-COUNT-SYSTEM-RESETS`

Round: `1`

Approved plan SHA-256: `15c49323b5434881e27f357bd675d3d7dedd02911392e1d0a43904316f15bbd5`

Definitions SHA-256: `c8ad386eab45c0b72a6fe27fb14de10548df3358a423fae581f245e6da08954a`

$caveman full. Implement `T03` only. Preserve exact technical substance.

## Validated predecessors

- `T01`: validated; attempts 2
- `T02`: validated; attempts 2

## Contract

# Revision 8 contract

Implement approved revision 8 SHA-256 `15c49323b5434881e27f357bd675d3d7dedd02911392e1d0a43904316f15bbd5`. Preserve observable module-init counting, same-boot deduplication, approximate ABI waiver, RELEASE exclusions, ordinary module operations, one reversible `armhf` DEB, continuous image lock, restoration/quarantine rules. Never use MMIO, reset reasons, force operations, target APT, kernel/U-Boot patching, credential persistence.

## Interfaces

# Revision 8 interfaces

- Module owns boot guard, immutable pending record identity, retry, count, filter, reset, serialization.
- Bash frontend transports module requests only.
- SysV activation follows verified Debian 8 default runlevel `2`.
- Package removal handles loaded and absent module, deletes only validated generated records, restores baseline.
- Toolchain compiler/binutils run only through `./bin/lets toolchain`.
- Image mutation runs only inside one `./bin/lets sdlc lock-run` or `test-package` lock.

## Task

# T03: reproducibility and disposable image

Run regression/static tests. Build module and canonical DEB twice byte-identically. Under one SDLC image lock: copy, mount, install, test, remove/purge, verify exact restoration, unmount, verify source SHA. Quarantine on cleanup/restoration failure.
