{"criteria":[
  {"id":"R10-01","text":"`insserv -s` diagnostic only; native rc, exact links, generated graph membership/order, stop registration control install verdict."},
  {"id":"R10-02","text":"Native unregister rc zero, zero all-runlevel service links, exact original three-graph hashes/types/metadata; no direct graph content edits."},
  {"id":"R10-03","text":"One armhf package; authentic original baseline survives real upgrade/abort/recovery; corrected hooks replace broken hooks through ordinary dpkg; loaded/absent cleanup and conflicts safe."},
  {"id":"R10-04","text":"Recovery tooling binds authentic original baseline, rejects dirty rebaseline/copy overwrite, holds real lock, clears matched quarantine only after restoration, unmount, loop release, source-hash PASS; failures retain markers."},
  {"id":"R10-05","text":"Existing quarantined copy recovered through corrected upgrade/remove/purge with exact restoration; tooling reports recovery; later fresh disposable lifecycle PASS; source hash exact."},
  {"id":"R10-06","text":"Two identical module and canonical DEB builds; static, ABI, CLI, sync-fault, exact-once, native lifecycle, tooling tests PASS; RELEASE limits preserved."},
  {"id":"R10-07","text":"After prerequisites, exact target native graph gate and one bounded reboot yield module, guard, device, exactly plus one record, no duplicate/anomaly/taint change."},
  {"id":"R10-08","text":"Ordinary target remove/purge restores exact records, graphs, links, metadata, protected files, package namespace, connectivity; no force, cleanup reboot, credentials, residue."}
]}
